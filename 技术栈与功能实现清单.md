# 技术栈与功能实现清单

> 本文档覆盖桌面外壳（Electron）、后端（FastAPI + SQLite）、前端（Vue 3 + D3 + Element Plus）三层。

## 一、桌面与外壳

| 功能 | 实现 |
|---|---|
| 桌面应用壳 | `app.js` / `main.js`（Electron 主进程：窗口管理、IPC） |
| 安全预加载 | `preload.js`（contextBridge 暴露受限 API） |
| 一键启动脚本 | `启动服务.bat`（后端 8080 + 前端 5173）；`知识图谱.bat` |
| 文件 IPC 桥 | `server.js` + `preload.js` 的 ipcMain.handle / ipcRenderer.invoke |

## 二、后端（FastAPI + SQLAlchemy + Pydantic）

入口：`backend/app/main.py`（uvicorn app.main:app --port 8080）。CORS 允许 localhost:5173。SQLite 默认库。

### 路由

| 前缀 | 端点 | 作用 |
|---|---|---|
| /api/files | upload/list/detail/update/delete/delete-all/nodes/sync | 文件上传/列表/详情/更新/删除/全清/文件-节点/本地双写 |
| /api/knowledge | validate / infer / search | 校验、关联推理、字面搜索 |
| /api/graph | build / update / rebuild | 图谱构建/更新/全局重建 |
| /api/notes | save / validate / list / get / delete | 笔记保存/校验/列表/详情/删除 |
| /api/kb | 14 个端点 | 知识库管理（条目/关系/画像/锚定/文件关联） |

### 核心服务

| 模块 | 说明 |
|---|---|
| services/parser.py | 文档解析、知识点提取、实体归一合并 |
| services/vector_engine.py | 句向量检索（sentence-transformers / 余弦相似度） |
| services/inference.py | 四维打分（sim_text/sim_vector/sim_corpus/sim_topology）+ 阈值过滤 |
| services/validator.py | v2 知识校验（7 类 × 4 级严重度） |
| services/kb_index.py | 知识库索引/锚定/画像/相似度 |
| services/kb_import.py | 知识库文档解析与理解 |
| services/kb_link.py | 锚定落库、画像、文件/节点关联 |
| seed.py | 种子知识库（423 条） |
| models/models.py | SQLAlchemy ORM 数据模型 |

## 三、前端（Vue 3 + Pinia + Element Plus + D3 v7）

### Pinia 状态（10 个 store）

| Store | 关键职责 |
|---|---|
| fileStore | 文件列表与上传 |
| graphStore | 图谱节点/连线/过滤/孤儿收养/重排 |
| noteStore | 笔记与关联节点 |
| folderStore | 本地文件夹工作区双写 |
| groupStore | 分组管理 |
| configStore | 四维权重配置 |
| settingsStore | 主题设置 |
| kbStore | 知识库状态 |
| questionsStore | 问答状态 |
| indexedDB | IndexedDB 持久化 |

### 关键组件

| 组件 | 职责 |
|---|---|
| GraphCanvas.vue | D3 力导向图谱（缩放/拖拽/筛选/风格面板） |
| ControlPanel.vue | 左侧控制面板（分组/隔离/权重/图例/校验/插件） |
| KnowledgeWorkbench.vue | 知识工作台（全屏/推荐/wikilink） |
| WorkbenchEditor.vue | 编辑器（[[双向链接]]、镜像光标） |
| KnowledgeAnalytics.vue | 网络分析（度/介数/PageRank/连通分量） |
| KnowledgeBaseView.vue | 知识库页面 |
| KbOntologyCanvas.vue | 知识库本体图 |
| KnowledgeQA.vue | 知识问答 |
| KnowledgeReview.vue | 每日回顾（间隔重复） |
| KnowledgeTimeline.vue | 知识时间线 |
| NodeDetailDrawer.vue | 节点详情抽屉（含学习路径） |
| LinkTooltip.vue | 连线悬浮提示 |

## 四、增强功能

F1 局部图谱（1/2/3 跳）｜F2 知识网络分析仪表盘｜F3 智能连接推荐｜F4 [[双向链接]]｜F5 图谱交互筛选｜F6 轻量插件系统｜P1 学习路径/每日回顾｜P2 知识问答｜P3 知识时间线｜知识库层（锚定/画像/文件关联）

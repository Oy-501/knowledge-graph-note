# 技术栈与功能实现清单

> 本文档覆盖桌面外壳（Electron）、后端（FastAPI + SQLite）、前端（Vue 3 + D3 + Element Plus）三层，以及本轮新增的 6 个增强功能。

---

## 一、桌面与外壳

| 功能 | 实现 |
|---|---|
| 桌面应用壳 | `app.js` / `main.js`（Electron 主进程：窗口管理、IPC、`webContents` 拦截） |
| 安全预加载 | `preload.js`（`contextBridge` 暴露 `openFile`/`saveNote`/`pickWorkspaceDir` 等受限 API） |
| 一键启动脚本 | `启动服务.bat`（后端 `py start.py` + 前端 `npm run dev`，端口 8000/5173）；`知识图谱.bat`（启动知识图谱批处理） |
| 文件 IPC 桥 | `server.js` + `preload.js` 的 `ipcMain.handle` / `ipcRenderer.invoke` |

## 二、后端（FastAPI + SQLAlchemy + Pydantic）

入口：`backend/app/main.py`（`uvicorn app.main:app --port 8080`）。CORS 允许 `localhost:5173`/`127.0.0.1:5173`。SQLite 默认库（`settings.resolved_database_url`）。

### 路由（4 组，共 14 端点）

| 前缀 | 方法 + 路径 | 关键响应/作用 |
|---|---|---|
| `/api/files` | POST `/upload`、`GET ""`、`GET /{id}/detail`、`PUT /{id}`、`DELETE /{id}`、`DELETE /all`、GET `/nodes?file_id=`、POST `/sync`（本地文件夹双写） | 文件上传/列表/详情/更新/删除/全清/文件-节点/本地双写 |
| `/api/knowledge` | POST `/validate`（v2 校验）、POST `/infer`（推理连线）、GET `/search?keyword=`（ilike 字面匹配） | 校验、关联推理、字面搜索 |
| `/api/graph` | POST `/build`、`POST /update`（权重阈值）、POST `/rebuild` | 图谱构建/更新/全局重建 |
| `/api/notes` | POST `/save`（含 `linked_nodes`）、POST `/{id}/validate`、GET ``、GET `/{id}`、DELETE `/{id}` | 笔记保存（含关联落库）/校验/列表/详情/删除 |

### 核心服务

| 模块 | 关键方法 |
|---|---|
| `services/parser.py` | `parse_document(bytes, filename) → text`（多格式文档解析） |
| `services/vector_engine.py` | 句向量检索（sentence-transformers / 余弦相似度） |
| `services/inference.py` | 四维打分（`sim_text`/`sim_vector`/`sim_corpus`/`sim_topology`）+ 阈值过滤 |
| `services/validator.py` | v2 知识校验（4 级 SEV：critical/major/minor/info） |
| `seed.py` | `seed_knowledge_base`（307 条种子知识，幂等引导） |
| `task_queue.py` | `AsyncTaskQueue`（单并发 worker） |
| `models/models.py` | 9 张表 SQLAlchemy ORM（User/File/KnowledgeBase/Node/KpEdge/DocEdge/Note/LinkEdge/ValidationReport） |

## 三、前端（Vue 3 `<script setup>` + Pinia + Element Plus + D3 v7 + @vueuse/core）

### 构建
`kg-vue3/vite.config.js`：端口 5173、Vue 插件、`@` → `src`、D3 子模块预构建、`manualChunks` = `d3` / `vendor`、`asyncComponent` 懒加载 `KnowledgeWorkbench`（81KB 按需）。

### Pinia 状态（8 个 store）

| Store | 关键 state / getter / action |
|---|---|
| `fileStore` | files, fileCount; `loadPersisted`, `addFile`, `removeFile` |
| `graphStore` | nodes, links, version, **graphFilters**（F5: colorMode/sizeMode/folders/tags/relationTypes）, **folderOptions/tagOptions/relationTypeOptions**（F5）, linkStats, validationStats, searchNodes; `loadFromBackend`, `rebuildAll`, `setGraphFilters`（F5） |
| `noteStore` | notes, currentNote; `createNote`, `saveNote`, `linkNode`（`note.linkedNodes`）, `unlinkNode` |
| `folderStore` | currentWorkspace, openNote, saveState; `init`（File System Access API 恢复句柄）、`saveNote`（双写 + 800ms 防抖）、`openNote` |
| `groupStore` | groups, currentGroupId, groupColorMap; `switchGroup`, `getHiddenNodeIds` |
| `configStore` | w_alpha/beta/gamma/delta, threshold, corpusEnabled; 关联四维权重 |
| `settingsStore` | settings, isDark; `toggleTheme`（持久化 + 220ms 一次性过渡类） |
| `indexedDB` | `openDb` + 各表回退读写 |

### 前端 API 层（`src/api/index.js`）
- `fileAPI`：`upload/delete/list/getNodes/syncLocal/renameFile`
- `knowledgeAPI`：`validate/inferLinks/search`（ilike）
- `graphAPI`：`build/update`
- `noteAPI`：`save/delete/list/get`

### 关键组件方法

| 组件 | 关键方法 |
|---|---|
| `App.vue` | 顶部导航（`Sunny/Moon` 主题切换） + 折叠左栏（`useLocalStorage('kg-left-sections', ['files','control','analytics'])`） + 工作台 `defineAsyncComponent` 懒加载 |
| `KnowledgeWorkbench.vue` | `useFullscreen(wbRoot)` 原生全屏 + `runMatching`（术语匹配 + 校验 + **推荐 recommender**） + **onWikilinkSync（F4）** + 编辑源路由（note/folder 双写） |
| `WorkbenchEditor.vue` | **F4 镜像 div 光标坐标 `getCaretPixel()`**、`detectWikilink`、`wlCommit`（替换为 `[[title]]`）、`scheduleWikilinkSync`（解析 `[[...]]` → 节点 id）、ghost 波浪线校准层（`syncGhostMetrics`/`syncGhostLayout`，不改字体度量）、`associateDialog` 右键关联 |
| `WorkbenchRightPanel.vue` | **F3 智能推荐 section**（`recommendations` prop + `dismissRecommend`）、文件/节点/校验三段折叠 + 关联选择弹窗 |
| `WorkbenchMiniGraph.vue` | **F1 viewMode（local/neighbor）** + `neighborIds` 1 跳扩展 + `seedIds` + 邻里节点灰色虚线渲染 |
| `GraphCanvas.vue` | D3 forceSimulation（d3-selection / d3-force / d3-zoom / d3-drag）、贝塞尔 `linkPath`、渐变 defs、**F5 风格面板**（颜色/大小/文件夹/标签/关系类型）、`nodeFill`/`nodeRadius` 三模式、孤立/收养高亮、`applyHighlight` hover |
| `ControlPanel.vue` | 12 个 panel-section（分组/隔离/四维权重/图例/校验/关联质量/异常监控/状态/健康/分组分布/插件） + **F6 插件系统 section**（注入 `setPluginContext` + `activatePlugins`） |
| `KnowledgeAnalytics.vue` | **F2 度中心度 + Brandes 介数 + 并查集连通分量 + 密度/平均度/孤立数** + Top 列表 + `el-dialog` 知识结构报告（自然语言 + 优化建议） |
| `LinkTooltip.vue` / `NodeDetailDrawer.vue` | 玻璃卡片 / 抽屉 |

### 服务/工具

| 文件 | 关键导出 |
|---|---|
| `services/recommender.js` | **F3 `recommendConnections(content, title, nodes, linkedIds, limit)`**——轻量启发式：提取术语 + 标题分词 → 与 node title/keywords/entities 命中打分 → top N |
| `utils/noteParser.js` | `extractTerms`（基于 100+ 常见软件工程术语库）、`matchGraphNodes`（title 包含/精确/关键词/描述）、`extractRelations`（关系关键词）、`parseMarkdownSummary`、`extractTags` |
| `utils/noteValidator.js` | v2 校验（4 级 SEV），返回 `{severity, type, description, correction, start, end}` |
| `utils/graphValidator.js` | `validateGraph`（孤儿/收养/环路）、`ensureNoOrphans`、`detectOrphans` |
| `utils/relationClassifier.js` | `getRelationColor` / `getRelationLineStyle`（六种关系类型 → 颜色/虚实） |
| `utils/resilience.js` | `TaskQueue` / `IntegrityGuard` / `debounce` / `logError` |

### 插件系统（F6）

| 文件 | 关键 API |
|---|---|
| `plugins/registry.js` | `registerPlugin(def)`（幂等）、`setPluginContext(ctx)`（注入 graphStore/noteStore/fileStore）、`createPluginAPI(id) → { registerExportFormat, registerCommand, getContext }`、`activatePlugins()` |
| `plugins/index.js` | 副作用导入内置插件 |
| `plugins/builtin/export-graph.js` | 「导出 JSON」「导出 Markdown」两个导出格式（Blob + `<a download>`） |
| `plugins/builtin/notes-stats.js` | 「统计笔记字数」命令（`ElMessage` 提示） |

## 四、本轮新增 6 个增强功能（要点）

| 编号 | 功能 | 关键实现 | 关键调用 |
|---|---|---|---|
| F1 | 局部图谱（深度 1/2/3 跳） | `WorkbenchMiniGraph.vue` + `depth` + `expandedNeighbors`（BFS 从种子向外扩展 depth-1 跳，返回 邻居 id→层数 Map） | `graphStore.links` 建邻接表 → BFS；扩展节点灰圆 + 虚线，半径随层递减 |
| F2 | 知识网络分析仪表盘 | `KnowledgeAnalytics.vue`（前端图论计算） | 度中心度、Brandes 介数（>300 跳过）、**PageRank 幂迭代 Top10**、并查集连通分量（`componentGroups`）、**断层区域**（2~4 节点小簇）、**薄弱环节**（度≤1 边缘节点）、密度、报告+建议 |
| F3 | 智能连接推荐 | `services/recommender.js`（启发式）+ `WorkbenchRightPanel.vue` 顶部「🔮 智能连接推荐」section | `extractTerms` + 标题分词 → 节点 `title/keywords/entities/description` 命中打分；`runMatching` 防抖调用；「＋ 关联」复用 `onLinkNode` |
| F4 | [[ 双向链接]] | `WorkbenchEditor.vue` 镜像 div 测光标 + 弹窗 + 链接解析同步 | `getCaretPixel()`（getComputedStyle 复制 textarea 度量，0px scroll 差值）、`detectWikilink`（未闭合 `[[`）、`wlCommit`（替换为 `[[title]]`）、`scheduleWikilinkSync`（解析所有 `[[..]]` 标题 → 节点 id → `emit('wikilink-sync', ids)`） |
| F5 | 图谱交互（颜色/大小/筛选） | `graphStore.graphFilters` + `GraphCanvas.vue` 风格面板 | `graphStore.setGraphFilters`、`nodeRadius` 三模式（degree/weight/fixed）、`nodeFill` 三模式（type/folder/single + `folderColor` 哈希）、`render()` 过滤：文件夹/标签/关系类型/**节点类型（user/kb/note）**/**关联强度阈值（minScore 滑条）** |
| F6 | 轻量级插件系统 | `plugins/registry.js` + 2 个内置插件 + `ControlPanel.vue` 插件区 | `registerPlugin`、`createPluginAPI` 暴露 `registerExportFormat/registerCommand`；`setPluginContext` 注入 stores；内置导出 JSON/Markdown + 笔记统计命令 |

## 五、P1 增强功能（本轮新增）

| 编号 | 功能 | 关键实现 | 关键调用 |
|---|---|---|---|
| P1-1 | 知识导出为学习路径 | `utils/learningPath.js` + `NodeDetailDrawer.vue` 学习路径 section | `buildLearningPath(targetId, nodes, links)`：沿 `relation_type==='prerequisite'` 边反向 DFS 收集前置链（source 高层→target 前置）；`groupByLevel` 按 level 分层；`toLearningPathMarkdown` 生成大纲；`downloadText` 浏览器下载 |
| P1-2 | 每日/每周知识回顾 | `KnowledgeReview.vue` + `App.vue` 左栏「每日回顾」collapse | 今日新增（`uploadTime`）、待处理校验（`validate.status`）、**间隔重复**（`useLocalStorage('kg-review-records')` + 间隔表 `[1,3,7,14,30,60]` 天，按度中心度排序推荐到期节点）、`onReviewed` 更新复习记录 |

## 六、P2 知识问答（轻量图遍历版）

| 编号 | 功能 | 关键实现 | 关键调用 |
|---|---|---|---|
| P2-1 | 轻量知识问答 | `utils/qa.js` + `KnowledgeQA.vue` + `App.vue` 左栏「知识问答」collapse | `answer(text, nodes, links)`：`findEntities`（节点标题包含，最长优先）识别实体 → ≥2 实体走 `findShortestPath`（BFS 无向图最短路径）；否则走 `findRelated`（`extractTerms`+`matchGraphNodes` 兜底标题包含）；`describePath` 生成逐跳关系+证据原文自然语言 |

## 七、P3 知识时间线（演变追踪）

| 编号 | 功能 | 关键实现 | 关键调用 |
|---|---|---|---|
| P3-1 | 知识时间线 | `KnowledgeTimeline.vue` + `NodeDetailDrawer.vue` 集成 | 从 `node.uploadTime`（首次出现）、相关 `links`（关联建立，用对端节点 uploadTime 近似时间）、`node.validate.confirmedAt`（校验确认）、`reviewRecords`（复习轨迹）构建事件列表，按时间排序渲染垂直时间线 |

## 八、知识库层（KB Layer，本轮新增）

> 定位：**知识库是系统的知识权威源**。图谱建立在知识库之上，文件之间、知识点之间的关联
> 由知识库概念空间决定，不再靠关键词/文本向量比相似度；未被知识库覆盖的内容不参与关联。
> 详见 `知识库设计说明.md`。

### 数据模型（新增 4 张表）

| 表 | 作用 |
|---|---|
| `kb_relations` | 知识库内部关系边（本体图）：`source_entity`/`target_entity`/`relation_type`/`weight`/`evidence`/`origin`(derived\|import\|manual) |
| `kb_imports` | 知识库文档上传记录 + 完整理解报告（`report` JSON） |
| `file_knowledge_profiles` | 文件知识画像：`concepts`/`domains`/`levels`/`anchor_count`/`concept_count`/`coverage` |
| `file_knowledge_links` | 文件间知识关联：`kb_similarity`/`direct_score`/`bridge_score`/`domain_score`/`shared_concepts`/`bridges` |

### 服务层

| 模块 | 关键方法 |
|---|---|
| `services/kb_index.py` | `KbIndex`（别名归一 / 最长匹配 `scan` / `build_profile` / `similarity` / `neighbors` / `specificity`）、`load_index`、`build_relations_from_entries` |
| `services/kb_import.py` | `detect_format`、`parse_kb_document`（md/表格/csv/tsv/json/txt，兼容 Excel BOM）、`understand_kb_document`（新增·同义合并·定义冲突·未采纳 + 关系落库） |
| `services/kb_link.py` | `apply_kb_anchoring`（补 entities、以知识库为准校正领域/层级、写 kb 来源）、`profile_file`、`build_profiles`、`link_files`/`link_files_for`（文件级）、`link_nodes_by_kb`（节点级）、`purge_kb_links`、`kb_rebuild_all`、`kb_incremental_for_file` |
| `scripts/kb_build.py` | CLI：`--relations-only` / `--sync-corpus` / `--understand <file>` / `--sample <text>` / 默认全量重建 |

### API（`/api/kb`，14 端点）

`GET /stats`、`GET /entries`、`POST|PUT|DELETE /entries[/{id}]`、`GET /relations`、`POST /relations`、`DELETE /relations/{id}`、
`GET /neighbors`、`POST /upload`（dry_run 预览）、`POST /preview`、`GET /imports[/{id}]`、`POST /match`、`GET /graph`、`GET /files`、`GET /file/{id}`、`POST /rebuild`

### 关联算法（无文本向量参与）

```
kb_similarity = 0.50 × direct   共享知识点（归一化概念向量余弦，×1.6 温和提升）
              + 0.35 × bridge   知识库关系图扩展向量余弦增量（1~2 跳，乘区分度 1/(1+log2(1+度))）
              + 0.15 × domain   领域分布重合 ×0.6 + 层级接近度 ×0.4（general 视为未标注）
```

节点级连线：同一知识点 → `equivalent 等价知识点`；知识库关系边两端 → 按 KB 关系映射为
`prerequisite / theory / comparison / bridged / related`；未锚定 → **不连线**。
落库标记 `source_file='知识库'` + `semantic_bridge=True`，可整体清除重建。

### 前端

| 文件 | 说明 |
|---|---|
| `store/kbStore.js` | 8 组 action：stats / entries / relations / graph / upload 理解 / match / files / rebuild |
| `components/KnowledgeBaseView.vue` | 四个区块：概览（统计+领域层级+本体图+重建）、上传理解（拖拽→预览报告→导入+历史）、知识点（增删改查+邻域下钻）、知识关联（锚定测试+文件画像+文件间关联证据+未覆盖告警） |
| `components/KbOntologyCanvas.vue` | d3-force 渲染知识库本体图（领域/层级着色、孤立点淡显、hover 卡片） |
| `api/index.js` | 新增 `kbAPI`（15 个方法） |
| `utils/relationClassifier.js` | 新增 `equivalent`（等价知识点）、`bridged`（知识库桥接）两类关系配色 |
| `App.vue` | 顶部导航新增「知识库」页签（懒加载 `KnowledgeBaseView`） |

### 配套修复

- `main.py` CORS 增加 `allow_origin_regex` 放行本机任意端口（vite 换端口不再导致接口被拦）。
- 泛化别名停用表（`类型/结构/主题/数据/框架…` 作为别名时不参与锚定），修掉「动态弱类型」误锚定「体裁」这类错误。
- 高度数知识点降权（区分度系数），避免枢纽概念把任意两个文件连起来。


## 九、图谱总结与导出（Summary → Flowchart → AI → Doc/PPT，本轮新增）

> 把网状的图谱收敛成可执行的流程，并让它"机器可读"：流程图既能给人看，也能投喂大模型。
> 详见 `图谱总结与导出说明.md`。

### 服务层

| 模块 | 关键方法 |
|---|---|
| `services/graph_summary.py` | `build_summary`（层级/领域/枢纽/并查集连通簇/**学习主线**/知识库桥接/孤立点/建议）、`_build_learning_paths`、`to_mermaid`（按阶段或领域分组 + classDef 配色 + 知识库桥接虚线）、`to_ai_digest`（token 友好的固定结构摘要）、`attach_raw_links`、`_looks_noisy`（抽取噪音标题识别） |
| `services/summary_export.py` | `to_markdown`（十节文档）、`to_docx`（Word，微软雅黑 + 表格）、`to_pptx`（10 页 16:9，**第 7 页用形状+箭头原生绘制流程图**）、`to_ai_digest_file`、`export_path`（`backend/exports/`，自动保留最近 30 个） |
| `scripts/graph_summary.py` | CLI：默认打印概览 / `--ai` / `--mermaid` / `--export md docx pptx ...` / `--demo`（用 `test_data/kb_demo` 搭临时库，不动正式库） |

### API（`/api/summary`，4 端点）

`POST /build`（返回 summary + mermaid + ai_digest）、`GET /mermaid`、`GET /ai-digest`、
`GET /download?fmt=md|docx|pptx|mermaid|digest|json&title=`

### 关联算法

- 学习主线：起点 = 层级靠前 + 入边少 + 度数高；前进按关系优先级
  `前置 → 理论 → 实现 → 延伸 → 演进 → 应用 → 对比 → 等价 → 桥接 → 弱关联`，不回访。
- 知识簇：并查集连通分量（≥2 节点）。
- 建议触发：孤立过多 / 缺 L1 骨架 / 枢纽过载 / 弱关联 > 60% / 知识库领域未覆盖 / 标题疑似噪音。

### 前端

| 文件 | 说明 |
|---|---|
| `store/summaryStore.js` | 生成总结、导出 6 种格式（blob 下载）、复制 Mermaid/AI 摘要 |
| `components/GraphSummaryView.vue` | 左：总览/学习主线/枢纽/知识簇/层级+领域/桥接/孤立/建议；右：流程图 + 双页签源码框 + 缩放；顶部：分组方式/主线数/导出按钮组 |
| `components/MermaidFlow.vue` | **动态 import mermaid**（单独 chunk，不进主包）渲染流程图，失败降级提示 + 保留源码 |
| `api/index.js` | 新增 `summaryAPI`（build / mermaid / aiDigest / downloadUrl / download） |
| `App.vue` | 顶部导航新增「图谱总结」页签（懒加载 `GraphSummaryView`） |

### 依赖

后端 `python-pptx==1.0.2`、`python-docx==1.2.0`；前端 `mermaid`（动态 import）。
## 十、大文件上传保护（防"上传大文件网站崩溃"，本轮新增）

> 根因是四层叠加：整份读进内存、解析无上限、关联推理 O(n²) 且每对一次 SQL、前端全量渲染。
> 详见 `大文件上传保护说明.md`。

### 配置（`app/config.py`，可用 `.env` 覆盖）

`MAX_UPLOAD_MB=20`、`MAX_PARSE_LINES=20000`、`MAX_NODES_PER_FILE=600`、`MAX_LINK_CANDIDATES=300`、
`LINK_PAIR_BUDGET=120000`、`LINK_TIME_BUDGET_S=20`、`MAX_LINKS_PER_NODE=8`、`LINK_OUTPUT_LIMIT=4000`、
`MAX_NODES_FOR_KB_LINK=4000`

### 后端

| 模块 | 改动 |
|---|---|
| `api/files.py` | `_read_text_limited()`：只读 `limit+1` 字节，超限 **413**（不进内存）；含 NUL → **415** 提示转文本；`GET /files/limits` 暴露上限；`/files/sync` 同样限量；文件列表返回 `parse_note` / `size` |
| `services/parser.py` | `extract_nodes_from_text` 返回 `(nodes, trunc)`，按 `max_lines`/`max_nodes` 截断；`parse_and_extract` 写 `files.parse_note` |
| `services/inference.py` | 知识库改**内存索引**（一次 `load_index`，消除每对一次 SQL）；倒排索引**候选集剪枝**；配对数/时间/输出三重预算 + 每节点最多 8 条；`/knowledge/infer` 亦受节点上限保护 |
| `services/vector_engine.py` | `embed_to_db` 由「每节点一次 SELECT」改为 `IN (...)` 批量回填 |
| `services/kb_link.py` | 文件配对 `MAX_FILES_FOR_LINKING=150` + 15s 预算；节点级锚定连线 4000 节点上限 |
| `models/models.py` + `database.py` | 新增 `files.parse_note` 列并写入列迁移清单 |

### 前端

| 文件 | 改动 |
|---|---|
| `store/fileStore.js` | 上传前按后端上限校验体积/类型（`fileAPI.limits()`），超限跳过并给出建议；轮询超时 120s→180s；带回 `parse_note`；`maxUploadMB` 状态 |
| `components/FileUploader.vue` | 拖拽区显示「单文件 ≤ 20MB · 仅纯文本」；文件列表显示 **⚠ 已截断** 徽标（hover 看原因）；accept 扩展 csv/tsv/json/log |
| `components/GraphCanvas.vue` | 大图谱渲染降级：>600 只画 260 个高度节点标签，>1200 只画 120 个并省略分组标签；>800 降低斥力、加快收敛 |
| `store/graphStore.js` | 图谱 >1200 节点时提示一次，建议用分组/筛选聚焦 |
## 十一、智能切割 · 知识点判定 · 后台管理 · 个人主页（本轮新增）

> 详见 `智能切割与知识点判定说明.md`。

### 新增数据模型

| 表 | 作用 |
|---|---|
| `knowledge_candidates` | 候选知识点池：来源文件/片/行号、原文、抽取置信度、判定分数与决定、**证据链 JSON**、判定阶段（auto/human）、裁决人与备注、入库条目 id |
| `audit_logs` | 操作审计：actor/action/target/summary/**detail(依据明细 JSON)**/status/duration_ms |
| `user_profiles` | 个人主页：昵称、简介、头像 URL、背景图 URL、背景参数 |
| `nodes.chunk_index`（新列） | 该知识点来自切割后的第几片 |

### 新增服务

| 模块 | 关键方法 |
|---|---|
| `services/file_splitter.py` | `plan_split`（标题/段落边界优先 + 单次预扫描得标题上下文 + 相邻片重叠）、`piece_text`（带上下文前缀）、`describe_plan`、`summarize_pieces` |
| `services/knowledge_verifier.py` | `verify_candidate`（四路证据加权：知识库一致性/图谱证据/文本质量规则/联网证据）、`Verdict`（分级判定 + 人类可读理由）、`apply_verdict`（落库 + 自动入库 + 审计）、`verify_batch`（含联网预算）、`_ingest_to_kb` |
| `services/web_probe.py` | `search_web`（Bing/搜狗/360/百度按序尝试，通用标题块提取 + 全文兜底 + 结果缓存 15min）、`supports`（检索结果是否支持该概念）、优雅降级 `online=False` |
| `services/audit.py` | `log_event`（永不抛异常）、`trim`（保留最近 N 条）、`summarize_action`、`recent_stats` |
| `scripts/kb_build.py --reparse` | 按最新规则重解析全部文件（顺带生成候选与判定） |

### API

- `/api/profile`：`GET ""`、`GET /overview`、`PUT ""`、`POST /avatar`、`POST /background`、`DELETE /background`
- `/api/admin`（`X-Admin-Token` 保护）：`/auth`、`/overview`、`/candidates`（列表/详情/单条裁决/批量裁决/重跑判定/删除）、`/audit`（列表/详情）、`/users`、`/stats/verdicts`、`/files`
- `/api/files/upload` 返回体新增 `split: {needed, pieces[], message}`；`/api/files/limits` 返回切割阈值
- `/uploads/*` 静态目录（头像/背景图）

### 前端

| 文件 | 说明 |
|---|---|
| `components/ProfileView.vue` | 个人主页：头像上传、昵称/简介、背景图上传与参数（不透明度/模糊/范围）、我的数据概览、后台入口 |
| `components/AdminView.vue` | 后台管理：口令闸门 + 概览（规模/判定分布/近 7 天操作/分数分布）+ 知识点审阅（筛选/证据链抽屉/单条与批量裁决/重跑判定）+ 操作审计（台账 + 依据明细 JSON）+ 用户与文件 |
| `api/index.js` | 新增 `profileAPI`（含 `fileUrl` 补全服务端图片地址）与 `adminAPI`（13 个方法，携带 `X-Admin-Token`） |
| `store/fileStore.js` | 硬上限改为 100MB、回显切割方案、`splitTargetLines` 状态 |
| `App.vue` | 导航新增「个人主页」「后台管理」（懒加载） |

### 配置

`backend/.env` 已生成：`ADMIN_TOKEN`、`SPLIT_*`、`MAX_UPLOAD_MB_HARD`、`VERDICT_*`、`UPLOAD_DIR`、`MAX_IMAGE_MB`、`AUDIT_KEEP_ROWS`。
## 十二、代码防御机制与 UI 优化（本轮新增）

> 目标：错误要么启动前被发现，要么发生时不扩散、不静默、可追溯。详见 `代码防御与UI优化说明.md`。

### 四层防线

| 层 | 落点 | 关键实现 |
|---|---|---|
| ① 开发期静态 | `backend/scripts/check.py`、`体检.bat` | 语法（内置 `compile()`，不落盘）/ 导入 / **路由重复冲突** / **模型列 vs 实际库列漂移** / 静默吞异常（AST 扫描）/ 关键配置 / 前端工程；`--json`、`--build`；退出码区分可启动与否 |
| ② 运行期兜底 | `app/services/errors.py`、`app/main.py` | `AppError`/`HTTPException`/`ValidationError`/`Exception` 四个处理器；`request_id` 中间件（响应头+响应体+日志+审计）；慢请求告警；**5xx 落审计**（后台可见）；`describe_exception` 按「报文关键字优先于异常类型」翻译成人话并给建议 |
| ③ 数据与边界守卫 | `errors.clamp_paging` | **修掉 `min(limit,200)` 不防负数**（SQLite `LIMIT -1` = 不限制 → 整表返回），8 处调用点统一替换；另有 `require_text`/`require_int`/`GuardError`/`safe_detail`（键名脱敏） |
| ④ 前端 | `main.js`、`ErrorBoundary.vue`、`utils/errorReporter.js`、`api/index.js` | `app.config.errorHandler`（带组件名+生命周期阶段）/ `window.onerror` / `unhandledrejection`；`ErrorBoundary` 包住 6 个视图且**自动重试封顶 2 次**（防错误震荡）；上报后端落审计（去重 30s + 限流 10/min + 静默失败）；`ApiError` 分类 + GET 幂等重试 1 次 + 5xx 上报 |

### 新增接口

`GET /api/system/doctor`（11 项体检，逐项 status/detail/hint/ms）
`GET /api/system/health-deep`（精简探活）
`POST /api/system/client-error`（前端错误回收入口 → 审计）

### 新增/修改文件

| 文件 | 说明 |
|---|---|
| `backend/app/services/errors.py` | 新增：错误模型、异常→人话映射、响应体构造、数据与分页守卫 |
| `backend/app/api/system.py` | 新增：体检 / 探活 / 前端错误回收 |
| `backend/scripts/check.py` | 新增：七项静态体检 |
| `体检.bat` | 新增：一键体检 |
| `backend/app/config.py` | 新增 `resolved_upload_dir` 属性（**上传目录单一事实来源**） |
| `backend/app/main.py` | 四个异常处理器 + request_id 中间件 + 5xx 审计 + 慢请求告警 |
| `backend/app/services/vector_engine.py` 等 6 个文件 | **11 处 `except: pass` 全部补日志**（向量降级改为 warning 并禁用坏编码器） |
| `backend/app/api/admin.py`、`kb.py` | 分页守卫替换（8 处）；`admin.py` 新增「系统自检」页签数据来源 |
| `kg-vue3/src/utils/errorReporter.js` | 新增：前端错误上报（去重/限流/keepalive/静默） |
| `kg-vue3/src/components/ErrorBoundary.vue` | 改造：原组件从未被使用；新增重试封顶、退回入口、上报说明 |
| `kg-vue3/src/main.js` | 注册全局错误处理器与全局钩子 |
| `kg-vue3/src/api/index.js` | `ApiError` 分类、hint 透传、GET 幂等重试、5xx 上报、`systemAPI` |
| `kg-vue3/src/App.vue` | 6 个视图全部套 ErrorBoundary；导航改声明式 `NAV_TABS`；`Ctrl/⌘+1~6` 快捷键；`activate()` 带滚入可视区 |
| `kg-vue3/src/components/AdminView.vue` | 新增「系统自检」页签（11 项清单 + 建议） |
| `kg-vue3/src/styles/main.css` | `:focus-visible` 焦点环、`prefers-reduced-motion` 降级、`::selection`、`.sr-only`；导航对比度 2.79→5.9 并支持横向滚动与快捷键徽标 |

### UI 优化要点

- **导航激活项对比度 2.79:1 → 约 5.9:1**（`accent-light` → `accent-strong`），并叠加内描边不单靠颜色区分
- 非激活页签 `text-muted`（约 2.4:1）→ `text-secondary`（约 4.5:1）
- 全站 `:focus-visible` 焦点环（此前为零，纯键盘用户无法判断焦点）
- 导航补齐 `role=tablist/tab`、`aria-selected`、`aria-controls`、图标按钮 `aria-label`
- 6 页签横向溢出滚动 + 切换时滚入可视区
- `prefers-reduced-motion` 关闭全部动效
## 十三、安全审计与加固（按七条安全原则，本轮新增）

> 原则：最小权限 / 攻击面最小化 / 纵深防御 / 默认安全 / 输入永不信任 / 零信任 / 失效安全。
> 审计 16 项、修复 15 项。详见 `安全审计与加固说明.md`。

### 高危三项

| 问题 | 实测证据 | 修法 |
|---|---|---|
| **存储型 XSS**：`WorkbenchEditor` 的 Markdown 预览完全不转义即 `v-html` | 注入 `<img src=x onerror=...>` → `window.__xssFired === 1`，脚本真的执行 | **先整体转义再套 Markdown 标签** + 偏移映射（`escapeWithMap`）+ 链接协议白名单；Mermaid `securityLevel` loose→**strict** |
| **30+ 写端点零鉴权**（含 `DELETE /api/kb/entries/{id}`、`POST /api/kb/rebuild`） | `POST /api/kb/rebuild` 无口令返回 **200** | **集中式写守卫中间件**：默认拒绝，豁免须显式登记并写理由；前端自动携带口令 + 首次 401 引导输入并自动重试 |
| **失效安全违规**：`require_admin` 口令为空时 `return True`（后台全开） | `ADMIN_TOKEN=""` 时 `/api/admin/overview` 返回 200 | 改 **503 拒绝**；比较改 `hmac.compare_digest`；失败限流 20 次/5 分钟（实测 20×401 后 5×429） |

### 中低危

| 项 | 修前 | 修后 |
|---|---|---|
| 接口文档 | `/docs` `/redoc` `/openapi.json` 生产可访问 | 404（`DEBUG=false` 时）；路由 63→59 |
| 监听地址 | `HOST` 默认 `0.0.0.0` | `127.0.0.1` |
| doctor 信息泄露 | 暴露绝对路径/平台指纹 | 未授权脱敏（`<路径已隐藏>`），带口令才给细节 |
| `X-Request-ID` | 原样入日志（可注入换行伪造日志行） | 白名单字符 + 截断 64 |
| LIKE 通配符 | 搜 `%` 命中**全表 423 条** | 按字面量；正常关键词结果不变 |
| 路径归属判断 | 子串包含（同前缀兄弟目录可绕过） | `realpath` + `commonpath` |
| 错误上报接口 | 无鉴权无限流，可刷爆审计表 | 免口令但限流 30 条/分钟 |

### 顺手修掉的真实缺陷：删除文件 500

`DELETE /api/files/2` 报 `FOREIGN KEY constraint failed` ——
`_purge_file_nodes` 漏清 `FileKnowledgeProfile` / `FileKnowledgeLink`
（引用 `files.id` 的 4 张表只清了 2 张）。已补齐，
并新增**外键清理覆盖**自动检查防止复发。

### 新增/修改文件

| 文件 | 说明 |
|---|---|
| `backend/app/services/security.py` | 新增：恒定时间口令校验、失效安全判定、滑动窗口限流、请求头净化 |
| `backend/app/main.py` | 写守卫中间件；**CORS 移到所有中间件之后（必须最外层）**；未捕获异常改在 CORS 内侧返回结构化 500；关闭生产文档 |
| `backend/app/api/admin.py` | `require_admin` fail-closed + `compare_digest` + 失败限流 |
| `backend/app/api/system.py` | doctor 分级脱敏；client-error 限流；默认口令升为 error 级 |
| `backend/app/api/files.py` | 补齐外键清理 |
| `backend/app/api/{kb,knowledge,admin}.py` | LIKE 通配符转义（`escape_like` + `escape='\\'`） |
| `backend/app/api/profile.py` | 路径归属判断改 `realpath` + `commonpath` |
| `backend/app/config.py` | `PROTECT_WRITES`、`ADMIN_FAIL_LIMIT/WINDOW`；`HOST` 默认回环 |
| `backend/scripts/check.py` | 新增 6 项：写保护 / 暴露面 / 认证守卫（行为验证）/ 前端注入防护 / **未绑定引用** / **外键清理覆盖** |
| `kg-vue3/src/api/index.js` | 自动携带口令；401 引导输入并自动重试（兼容 `AxiosHeaders`） |
| `kg-vue3/src/components/WorkbenchEditor.vue` | Markdown 渲染改「先转义再套标签」+ 偏移映射 + 链接协议白名单 |
| `kg-vue3/src/components/MermaidFlow.vue` | `securityLevel: 'strict'` |

### 遗留风险（未做）

口令存 `localStorage`（彻底修需服务端会话机制）；无用户体系（固定 `user_id=1`）；
限流为单进程内存实现；`backend/` 下有 4 份数据库副本（在 `.gitignore` 内，按最小权限建议清理）。

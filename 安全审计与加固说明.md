# 安全审计与加固说明

> 按 7 条原则（最小权限 / 攻击面最小化 / 纵深防御 / 默认安全 / 输入永不信任 / 零信任 / 失效安全）
> 对本项目做了一次对照审计。
>
> 本文只写**已核实的事实**：每条结论都有对应代码位置或实测证据。
> 已知的遗留风险单独列在第六节，不粉饰。

---

## 一、审计结论速览

| # | 违反的原则 | 问题 | 严重度 | 状态 |
|---|---|---|---|---|
| 1 | 输入永不信任 / 纵深防御 | Markdown 预览**完全不转义**即 `v-html` → **存储型 XSS**，实测脚本已执行 | **高** | 已修 |
| 2 | 输入永不信任 / 纵深防御 | Mermaid `securityLevel: 'loose'` + `innerHTML`，图源来自上传文档 | **高** | 已修 |
| 3 | 失效安全 | `require_admin` 在口令为空时 **fail-open**（放行整个后台） | **高** | 已修 |
| 4 | 零信任 / 最小权限 | **30+ 个写端点零鉴权**，含 `DELETE /api/kb/entries/{id}`、`POST /api/kb/rebuild`；实测 `POST /api/kb/rebuild` 无口令返回 **200** | **高** | 已修 |
| 5 | 默认安全 | `ADMIN_TOKEN` 硬编码默认 `kg-admin` | **高** | 已修（升为 error 级告警，需用户改） |
| 6 | 攻击面最小化 | `/docs`、`/redoc`、`/openapi.json` 生产环境也开放 | 中 | 已修 |
| 7 | 攻击面最小化 | `HOST` 默认 `0.0.0.0`（`py -m app.main` 会绑全网卡） | 中 | 已修 |
| 8 | 零信任 | `/api/system/doctor` 无鉴权且暴露绝对路径、平台指纹 | 中 | 已修 |
| 9 | 零信任 / 输入永不信任 | 后台口令可无限次尝试（在线暴力破解） | 中 | 已修 |
| 10 | 零信任 | 前端后台口令存 `localStorage`（XSS 可直接读） | 中 | 见遗留风险 6.1 |
| 11 | 输入永不信任 | `X-Request-ID` 请求头原样写日志/审计（日志注入） | 低 | 已修 |
| 12 | 零信任 | `/api/system/client-error` 无鉴权 → 可刷爆审计表 | 低 | 已修 |
| 13 | 纵深防御 | `_cleanup_old` 用子串判断路径归属（同前缀兄弟目录可绕过） | 低 | 已修 |
| 14 | 输入永不信任 | LIKE 查询未转义 `%` / `_`（搜索 `%` 命中**全表 423 条**） | 低 | 已修 |
| 15 | 失效安全 / 纵深防御 | 删除文件报 `FOREIGN KEY constraint failed`（漏清两张子表） | **中** | 已修 |
| 16 | 最小权限 | 遗留数据副本散落（`feature.db` 11.5MB 等） | 低 | 见遗留风险 6.2 |

**已做对的地方**（未改动）：`.gitignore` 已排除 `*.db`、`backend/.env`、`backend/uploads/`；
无 `eval`/`exec`/`subprocess`/`pickle`；无字符串拼接 SQL（全部走 SQLAlchemy ORM）；
图片上传有魔数校验（拒绝 SVG，避免上传即 XSS）；`启动服务.bat` 已显式绑 `127.0.0.1`。

---

## 二、最严重的一条：存储型 XSS（已修）

### 攻击链

```
上传/编辑含 HTML 的文档  →  解析成节点标题/笔记内容
   →  Markdown 预览用 v-html 注入未转义的 HTML
   →  浏览器执行脚本  →  读取 localStorage 里的后台口令
   →  调用接口（当时全部无鉴权）删除知识库条目、重建知识库
```

### 实测证据（修复前）

在笔记里写入 `<img src=x onerror="window.__xssFired=1">`，切到预览：

```
XSS是否触发: 1
预览HTML: <h1>测试</h1><p><img src="x" onerror="window.__xssFired=1"></p>
          <p><script>window.__xssFired=2</script></p>
```

`onerror` 确实执行了。原因很直接 —— 渲染函数把原始文本直接套 Markdown 标签后交给 `v-html`：

```js
// 修复前：先套标签，全程不转义
let html = rawContent
html = html.replace(/^# (.+)$/gm, '<h1>$1</h1>')
...
return html            // → v-html
```

> 同一个文件里的编辑区校准层（`ghostHtml`）**一直是对的**，它用了 `escapeHtml`。
> 也就是「同一个人写的两个渲染路径，一个对了、一个漏了」——
> 这正是纵深防御要解决的：不能依赖「每处都记得」。

### 修法

**先整体转义，再套 Markdown 标签**（顺序不能反）。同时补两道：

1. **偏移映射**：校验波浪线的下标基于原文，转义会改变长度
   （`<` 1 字符 → `&lt;` 4 字符），直接沿用会错位。
   用 `escapeWithMap(src)` 建「原文下标 → 转义后下标」映射表后再标注。
2. **链接协议白名单**：`[点我](javascript:...)` 即使转义也仍是可点击脚本，
   转义防不住。生成 `href` 时校验协议，只放行 `http/https/mailto`、`/`、`#`。

### 实测（修复后）

```
XSS标志: 0
预览HTML: <h1>标题</h1>
  <p>&lt;img src=x onerror=&quot;window.__xssFired=1&quot;&gt;</p>
  <p>&lt;svg onload=&quot;window.__xssFired=3&quot;&gt;&lt;/svg&gt;</p>
  <p><a href="#" target="_blank" rel="noopener noreferrer">md链接</a></p>
  <p><strong>粗体</strong> 与 <code>代码</code></p>
```

脚本不执行、标签被转义、`javascript:` 链接被中和，
而 **Markdown 格式照常渲染**（标题/粗体/行内代码都正常）—— 没有功能代价。

Mermaid 侧：`securityLevel` 由 `'loose'` 改为 `'strict'`。

---

## 三、失效安全：口令为空时后台全开（已修）

```python
# 修复前
def require_admin(x_admin_token=None):
    expected = (settings.ADMIN_TOKEN or "").strip()
    if not expected:
        return True        # ← 口令没配 = 不设防，后台彻底开放
```

配置缺失属于「加固未完成」，应当**拒绝**而不是放行。
口令为空的场景并不罕见：`.env` 写错、被清空、换环境忘带。

同时修了两处认证弱点：

| 项 | 修复前 | 修复后 |
|---|---|---|
| 空口令 | `return True`（放行） | **503 拒绝** + 明确告知如何配置 |
| 比较方式 | `!= expected`（逐字符，泄露长度与前缀） | `hmac.compare_digest`（恒定时间） |
| 失败次数 | 无限制 | 20 次 / 5 分钟 → **429**（按客户端 IP） |

限流键刻意**不信任 `X-Forwarded-For`** —— 该头可伪造，信任它等于允许绕过限流。

### 实测

```
ADMIN_TOKEN="" →  /api/admin/overview      HTTP 503
                  任意口令访问                HTTP 503
                  写操作 POST /api/kb/rebuild HTTP 401
（修复前这三项都会 200 放行）

连续 25 次错误口令：20×401 后 5×429
```

---

## 四、零信任：写操作守卫（已修）

### 问题

除 `/api/admin/*` 外，**30 多个写端点完全没有鉴权**，其中包括：

- `DELETE /api/kb/entries/{entry_id}` —— 删除知识库条目
- `POST /api/kb/rebuild`、`POST /api/graph/rebuild` —— 重建/清空知识库
- `DELETE /api/files/{file_id}` —— 删除文件及其全部知识点
- `PATCH /api/files/{file_id}`、`POST /api/notes/save` …

实测（修复前）：

```
POST /api/kb/rebuild   → HTTP 200   ← 无口令即重建知识库
POST /api/graph/rebuild→ HTTP 200
```

### 修法：集中式写守卫中间件

```
POST/PUT/PATCH/DELETE  →  写守卫  →  校验 X-Admin-Token  →  业务
              ↑ 默认拒绝，豁免需显式登记并写明理由
```

**为什么用中间件而不是逐个端点加依赖**：逐个加必然遗漏（现实已经证明了）。
中间件是一处集中判定 —— 新增端点默认受保护，想豁免必须写进
`WRITE_GUARD_EXEMPT` 并注明原因（目前仅 1 条：前端错误上报通道，
因为报错时用户可能还没输过口令，豁免但严格限流）。
后台自身已有更强的校验，走 `WRITE_GUARD_PREFIX_EXEMPT`。

### 实测

| 请求 | 修复前 | 修复后 |
|---|---|---|
| `POST /api/kb/rebuild` 无口令 | 200 | **401** + 可执行建议 |
| `DELETE /api/files/2` 无口令 | 200 | **401** |
| 带正确口令 | 200 | 进业务逻辑（422/200） |

### 前端配套：一次输入，之后自动携带

为了不把安全成本转嫁给日常使用：

1. 请求拦截器自动附加 `localStorage` 里的口令；
2. 首次遇到 401 时弹一次口令输入框，输入后**自动重试原请求**，之后不再打扰。

实测闭环：清空口令 → 触发写操作 → 弹窗 → 输入 `kg-admin` → **重试成功 `status=200`**。

> 途中修掉一个隐蔽 bug：重试复用的 `config` 已被 axios 归一化成 `AxiosHeaders`，
> 此时**直接赋值普通属性不生效**，导致「用户输了口令却依然 401」。
> 改为优先用 `headers.set()`。

---

## 五、其余修复

### 5.1 攻击面最小化

| 项 | 修复前 | 修复后 |
|---|---|---|
| `/docs` `/redoc` `/openapi.json` | 始终开放（等于给攻击者接口地图） | `DEBUG=False` 时返回 **404**；需要时 `.env` 设 `DEBUG=true` |
| `HOST` 默认值 | `0.0.0.0`（可能暴露到局域网） | `127.0.0.1` |
| 路由总数 | 63 | 59（少掉的正是 4 条文档路由） |

### 5.2 信息泄露：`/api/system/doctor` 分级

该接口免口令（供探活使用），但此前会暴露数据库绝对路径、上传目录、导出目录、
Python/系统版本、口令是否默认。

改法：**未携带有效口令时脱敏**，带口令（后台「系统自检」页会自动携带）才给完整细节与建议。

实测：

```
无口令： authorized=false  上传目录项 = "可读写 · 2 个文件 · 0.00MB · <路径已隐藏>"
带口令： authorized=true   上传目录项 = "可读写 · 2 个文件 · 0.00MB · C:\Users\yr200\…"
```

### 5.3 输入永不信任：三处净化

| 输入 | 风险 | 修法 |
|---|---|---|
| `X-Request-ID` 请求头 | 客户端可塞换行符伪造日志行（日志注入） | 白名单字符 + 截断 64 位。实测 `abc123%0AFAKE-LOG-LINE<>"` → `abc1230AFAKE-LOG-LINE` |
| 搜索关键词 `keyword` | `%` / `_` 被当成通配符语法，搜索 `%` 命中全表 | `escape_like()` + `ilike(..., escape='\\')` |
| 上传路径 `old_url` | 子串判断路径归属，同前缀兄弟目录可绕过 | `realpath` + `commonpath` 严格前缀比较，越界记录告警 |

LIKE 转义实测（同一份数据）：

| 关键词 | 修复前 | 修复后 |
|---|---|---|
| `%` | **423 条（全表）** | 0（按字面量匹配） |
| `_` | **423 条（全表）** | 0 |
| `a` | 48 | 48（不变） |
| `TypeScript` | 2 | 2（不变） |
| `设计` | 5 | 5（不变） |

正常关键词结果**完全不变** —— 修复没有功能代价。

### 5.4 限流：前端错误上报

`/api/system/client-error` 免口令（否则「页面崩了→错误报不上去」），
因此按来源限流（30 条/分钟），避免刷爆审计表把真正有用的记录挤掉。

### 5.5 顺手修掉的真实缺陷：删除文件 500

验证写守卫时发现 `DELETE /api/files/2` 带口令后返回 **500**：

```
IntegrityError: FOREIGN KEY constraint failed
[SQL: DELETE FROM files WHERE files.id = ?]
```

原因：数据库开启了 `PRAGMA foreign_keys=ON`，删除父行前必须清子行。
清理函数 `_purge_file_nodes` 覆盖了 `Node` 与 `KnowledgeCandidate`，
但**漏了后来新增的 `FileKnowledgeProfile` 与 `FileKnowledgeLink`**。
报错点在 `DELETE FROM files`，完全看不出是哪张子表没清。

修法：补齐两张表的清理，并把「与节点无关的文件级清理」提到早退之前。
另外在体检脚本里加了一条**自动回归检查**：

> 解析 ORM 模型，找出所有外键指向 `files.id` 的模型，
> 再解析 `_purge_file_nodes` 收集其中 `db.query(Model)` 用到的类，
> 要求前者是后者的子集。

这条检查本身也踩过一次坑：第一版用字符串包含判断，
结果**被自己写的文档注释骗过**（注释里提到了表名就算通过），
导致人为制造的缺失检测不出来。改为只解析 AST 里真正的 `db.query(...)` 调用后，
反向验证有效（删掉一行清理语句即报错）。

> 附带说明：这个 500 能被一眼定位，靠的正是此前加的全局异常兜底 ——
> 结构化错误直接给出了异常类型、原始 SQL 与 request_id。
> 如果是原来的裸 500，这段排查要花久得多。

---

## 六、遗留风险（尚未解决，如实列出）

### 6.1 后台口令存在 `localStorage`

XSS 一旦出现可直接读取。本次已把 XSS 入口堵上，
但「口令存前端」本身就是风险面。**彻底修法**需要服务端会话机制
（HttpOnly Cookie + 短期会话令牌），属于一次较大的改造，本轮未做。

缓解现状：XSS 已封堵；口令只用于本机单用户；`/docs` 已关闭降低探测面。

### 6.2 遗留数据副本

`backend/` 下存在多份数据库副本（均在 `.gitignore` 保护范围内，不会提交）：

| 文件 | 大小 | 说明 |
|---|---|---|
| `kg.db` | 1.3MB | 正在使用 |
| `feature.db` | 11.5MB | 此前功能验证留下的测试库 |
| `knowledge_graph.db` | 2.6MB | 9 月 1 日的旧库 |
| `kg.db.bak-before-kb` | 0.5MB | 知识库改造前的备份 |

按最小权限，不需要的副本应当清掉（数据副本越多，泄露与误用的面越大）。
**未自动删除**：这些是你的数据，删之前需要你确认。

### 6.3 无用户体系

接口用固定 `user_id=1`，没有登录/会话/权限模型。
「最小权限」在当前设计下只能做到「本机可信 = 拥有全部权限」。
若将来要多人使用或多端访问，需要引入真正的认证与授权（角色、资源归属校验）。

### 6.4 其他

- 限流是**单进程内存**实现，多 worker 部署时失效，需换共享存储（Redis）。
- 口令未做强度校验（长度/复杂度），仅靠提示。
- `SAFE_METHODS` 之外的写守卫不区分「读语义的 POST」（如 `/api/kb/match` 只是计算），
  这类接口现在也要求口令 —— 出于「宁可多要一次口令」的选择，可接受。
- 未做 CSRF 防护：当前不使用 Cookie 认证，不构成 CSRF 面；
  将来若改用 Cookie 会话则必须补上。

---

## 七、日常怎么用

```bash
体检.bat            # 现在包含 4 项安全不变量检查
启动服务.bat         # 启动
```

### 需要你做的两件事

1. **改后台口令**：`backend/.env` 的 `ADMIN_TOKEN`，默认 `kg-admin` 太弱。
   体检会一直把这条标成 ✕ 直到你改掉。
2. **首次使用写操作时会弹一次口令输入框** —— 输入后本机记住，之后不再打扰。
   如果你确实只在本机单人使用且不想被打扰，可在 `.env` 里设
   `PROTECT_WRITES=false` 关闭写保护（**会显著降低安全性，不建议**）。

### 体检脚本新增的 4 项安全不变量

| 检查 | 断言内容 |
|---|---|
| 写操作保护 | `PROTECT_WRITES` 开启，且守卫中间件确实注册 |
| 暴露面收敛 | `DEBUG=false` + `HOST=127.0.0.1` + 口令非默认 |
| 认证守卫 | **行为验证**：口令未配置→拒绝、口令错误→拒绝；并用 `hmac.compare_digest` 与失败限流 |
| 前端注入防护 | 所有 `v-html`/`innerHTML` 位置都有配套转义/净化；Mermaid 为 `strict` |

> 认证那条刻意用**行为验证**而不是字符串匹配 ——
> 因为字符串匹配会被代码注释里的示例骗过（这个坑在本轮出现过两次：
> `require_admin` 的文档注释里引用了旧的错误写法，导致误报）。
> 判定逻辑本身也必须可靠，否则防线不可信。

另新增一项 **未绑定引用** 检查：`import app.main` 只执行模块顶层，
函数体里的名字要等**被调用**才解析。本轮改代码时把
`knowledge.py` 的 `escape_like` 导入改丢了，导入检查完全通过，
直到用户点到那个接口才会炸 `NameError`。
新检查通过「收集模块内所有绑定名 → 与所有 Load 引用做差集」把这类错误提前暴露。

---

## 八、本轮改动清单

| 文件 | 改动 |
|---|---|
| `app/services/security.py` | **新增**：恒定时间口令校验、失效安全判定、滑动窗口限流、请求头净化 |
| `app/main.py` | 写操作守卫中间件；**CORS 移到最外层**；未捕获异常在 CORS 内侧返回（见下）；关闭生产文档；`HOST` 默认回环 |
| `app/api/admin.py` | `require_admin` 改 fail-closed + `compare_digest` + 失败限流 |
| `app/api/system.py` | doctor 分级脱敏；`client-error` 限流；默认口令升为 error |
| `app/api/files.py` | 补齐外键清理（修复删除文件 500） |
| `app/api/{kb,knowledge,admin}.py` | LIKE 通配符转义 |
| `app/api/profile.py` | 路径归属判断改 `realpath` + `commonpath` |
| `app/services/errors.py` | 新增 `escape_like` |
| `app/config.py` | `PROTECT_WRITES`、`ADMIN_FAIL_LIMIT/WINDOW`；`HOST` 默认值 |
| `scripts/check.py` | 新增 4 项安全不变量 + 未绑定引用 + 外键清理覆盖 |
| `kg-vue3/src/components/WorkbenchEditor.vue` | Markdown 渲染改为「先转义再套标签」+ 偏移映射 + 链接协议白名单 |
| `kg-vue3/src/components/MermaidFlow.vue` | `securityLevel: 'strict'` |
| `kg-vue3/src/api/index.js` | 自动携带口令；401 引导输入并自动重试（兼容 `AxiosHeaders`） |

### 一个值得单独记下的坑：CORS 中间件必须在最外层

Starlette 的 `add_middleware` 是**后注册者在外层**。CORS 只给「经过它的响应」
补 `Access-Control-Allow-Origin`。如果 CORS 在内层，那么由外层中间件
**短路生成**的响应（写操作被拒的 401、兜底的 500）不带 CORS 头，
浏览器直接判定为跨域失败 —— 前端只能看到 `Failed to fetch / 无法连接后端服务`，
而精心构造的 message / hint / request_id 用户一个字都看不到。

这个坑的迷惑性在于：**用 curl 测接口完全正常**（curl 不检查 CORS），
只有浏览器里表现为「连不上后端」。所以 CORS 已移到所有中间件之后注册。

# 知识库设计说明（Knowledge Base Layer）

> 解决的问题：原系统的关联靠「关键词 + 文本向量 + TF-IDF」算相似度，本质是字面/语义近似，存在两个硬伤——换个说法就断链；表面像但知识上无关的内容被强行连线。现在改为：知识库是唯一的权威源，图谱建立在知识库之上，关联由知识库决定。

## 一、核心思想

| 环节 | 旧做法 | 新做法 |
|---|---|---|
| 文档理解 | 直接抽词切句 | 知识库别名索引最长匹配锚定 |
| 节点属性 | 启发式猜 | 以知识库 domain/level 为准 |
| 节点间关联 | Jaccard + 向量余弦 | 只连同一知识点或知识库图可达 |
| 文件间关联 | 文本向量余弦 | 知识画像概念空间比较 |
| 无依据内容 | 照样参与 | 不参与（知识边界） |

一句话：先认知识，再谈关系。

## 二、数据模型

- knowledge_base：entity/aliases/domain/level/definition/bridge_sentences/related_terms/opposite_terms（423 条种子）
- 新增 4 张表：kb_relations、kb_imports、file_knowledge_profiles、file_knowledge_links

## 三、五步流水线

① 知识库索引 → ② 知识锚定 → ③ 知识画像 → ④ 知识关联（kb_similarity）→ ⑤ 图谱落地

## 四、关键算法

### 锚定：全角→半角、小写、去空白；最长匹配长键优先；ASCII 词边界；泛化别名停用。

### 知识相似度

kb_similarity = 0.50 × direct + 0.35 × bridge + 0.15 × domain；bridge 支持 1/2 跳，乘区分度系数降权泛化枢纽；全流程零文本向量。

### 节点级连线：锚定同一知识点→equivalent；知识库有边→prerequisite/theory/comparison/bridged；未锚定→不连线。

## 五、API（前缀 /api/kb）

stats / entries / relations / neighbors / upload / preview / imports / match / graph / files / file/{id} / rebuild（14 端点）

## 六、支持 5 种知识库格式

Markdown 条目式｜Markdown 表格｜CSV/TSV｜JSON｜纯文本；理解报告：新增/同义合并/定义冲突/未采纳。

## 七、实测效果（5 份演示文档）

前端工程实践 锚定14次/覆盖率34.9%；技术选型讨论 20次/43.9%；服务端接口设计 11次/23.1%；写作与知识整理 3次/3.8%；自造概念实验 1次/1.4%。

文件关联：前端工程实践×技术选型讨论=0.43；技术选型讨论×服务端接口设计=0.41；自造概念×任意文件=不连。

## 八、使用方式

后端：py -m uvicorn app.main:app --host 127.0.0.1 --port 8080；命令行：py scripts/kb_build.py（--relations-only / --sample / --understand）；前端：「知识库」标签页。

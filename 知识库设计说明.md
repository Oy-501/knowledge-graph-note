# 知识库设计说明（Knowledge Base Layer）

> 解决的问题：原系统的关联靠「关键词 + 文本向量 + TF-IDF」算相似度，本质是**字面/语义近似**，
> 存在两个硬伤 —— ① 换个说法就断链（"JS" 与 "JavaScript" 各算各的）；
> ② 表面像但知识上无关的内容被强行连线（"动态弱类型"里的"类型"撞上写作领域的"体裁"）。
>
> 现在改为：**知识库是唯一的权威源，图谱建立在知识库之上，关联由知识库决定。**

---

## 一、核心思想

| 环节 | 旧做法（语义相似） | 新做法（知识库驱动） |
|---|---|---|
| 文档理解 | 直接抽词、切句作节点 | 先在知识库别名索引上做**最长匹配**，把内容**锚定**到知识点 |
| 节点属性 | 由文本启发式猜领域/层级 | 以知识库条目的 domain / level 为准 |
| 节点间关联 | 关键词 Jaccard + 向量余弦 | 只连"同一个知识点"或"知识库关系图上可达"的点 |
| 文件间关联 | 文本向量余弦 | 文件**知识画像**在知识库概念空间的比较 |
| 无知识依据的内容 | 照样参与关联 | **不参与关联**（知识边界） |

一句话：**先认知识，再谈关系。**

---

## 二、数据模型

### 1) 知识库本身（已有表，本轮启用为权威源）

- `knowledge_base`：知识点条目 —— `entity`（规范名）、`aliases`（别名）、`domain`、`level`（1 元概念 / 2 核心理论 / 3 应用实践 / 4 实现工具）、`definition`、`bridge_sentences`、`related_terms`、`opposite_terms`、`credibility`、`source`。
  项目启动时自动从 `domain_corpus/*.md` 导入种子条目（423 条）。

### 2) 本轮新增的 4 张表

| 表 | 作用 | 关键字段 |
|---|---|---|
| `kb_relations` | **知识库内部关系边**（本体图） | `source_entity`、`target_entity`、`relation_type`、`weight`、`evidence`、`origin`（derived/import/manual） |
| `kb_imports` | 用户上传知识库的**导入记录 + 理解报告** | `format`、`entry_total`、`new_count`、`merged_count`、`conflict_count`、`relation_count`、`rejected_count`、`report` |
| `file_knowledge_profiles` | 文件的**知识画像**（概念空间稀疏向量） | `concepts`、`domains`、`levels`、`anchor_count`、`concept_count`、`coverage` |
| `file_knowledge_links` | **文件间知识关联**（带证据） | `kb_similarity`、`direct_score`、`bridge_score`、`domain_score`、`shared_concepts`、`bridges` |

---

## 三、五步流水线

```
① 知识库索引      别名归一 → 倒排键表 → 知识点关系图（KbIndex）
        ↓
② 知识锚定        文档文本 → 最长匹配 → 命中的知识点（带原文位置/证据）
        ↓
③ 知识画像        文件 → 概念空间稀疏向量（只含被锚定的内容）
        ↓
④ 知识关联        共享知识点 / 知识库关系桥接 / 领域层级一致性 → kb_similarity
        ↓
⑤ 图谱落地        节点级连线（source_file='知识库'）+ 文件级关联供前端下钻
```

涉及代码：

| 文件 | 职责 |
|---|---|
| `backend/app/services/kb_index.py` | 索引 / 扫描锚定 / 画像 / 相似度（`KbIndex`、`load_index`、`build_relations_from_entries`） |
| `backend/app/services/kb_import.py` | 知识库文档解析与理解（md / csv / tsv / json / txt → 新增·合并·冲突报告） |
| `backend/app/services/kb_link.py` | 锚定落库、画像、文件/节点关联、全量与增量编排 |
| `backend/app/api/kb.py` | 知识库 API（14 个端点） |
| `backend/scripts/kb_build.py` | 命令行构建/体检工具 |
| `kg-vue3/src/store/kbStore.js`、`components/KnowledgeBaseView.vue`、`components/KbOntologyCanvas.vue` | 前端知识库页面（概览 / 上传理解 / 知识点 / 知识关联） |

---

## 四、关键算法

### 1) 锚定（Anchoring）

- 归一化：全角→半角（1:1 映射，保证字符偏移不变）、小写、去空白。
- **最长匹配**：按首字符倒排候选键、长键优先，命中后指针跳过整个词，避免嵌套重复。
- ASCII 词边界：`java` 不会命中 `javascript`。
- 泛化别名停用表：`类型 / 结构 / 主题 / 数据 / 框架 …` 作为**别名**时不注册为锚定键（知识点本身叫这些名字仍然可锚定）。

### 2) 知识相似度（`kb_similarity`）

```
kb_similarity = 0.50 × direct      （共享知识点：归一化概念向量的余弦）
              + 0.35 × bridge      （知识库关系图扩展向量的余弦增量）
              + 0.15 × domain      （领域分布重合 60% + 层级接近度 40%）
```

- `bridge` 支持 1 跳 / 2 跳通路，并乘**区分度系数** `1/(1+log2(1+度))`：
  越泛化的枢纽知识点（如"接口""结构"）在桥接中权重越低，避免"什么都能连"。
- `general` 领域视为"未标注"，不计入领域一致性。
- 全流程零文本向量参与（即使没装 sentence-transformers 也完全一致）。

### 3) 节点级连线规则

| 情形 | 关系类型 | 证据示例 |
|---|---|---|
| 两个节点锚定同一知识点 | `equivalent` 等价知识点 | 两个知识点均锚定知识库条目「TypeScript」 |
| 两个知识点在知识库中有关系边 | 按 KB 关系映射为 `prerequisite / theory / comparison / bridged / …` | 知识库桥接：JavaScript —related→ TypeScript |
| 未被知识库锚定 | **不连线** | —— |

节点级连线统一写入 `links` 表，`source_file = '知识库'`、`semantic_bridge=True`，可一键清除重建（不影响用户手工确认的连线）。

---

## 五、API 一览（前缀 `/api/kb`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/stats` | 总览：条目/别名/关系/领域/层级/上传记录/覆盖率/关联数 |
| GET | `/entries` | 条目列表（keyword / domain / level / 分页） |
| POST / PUT / DELETE | `/entries`、`/entries/{id}` | 知识点新增 / 编辑 / 删除（删除会清理关系边） |
| GET | `/relations` | 知识库关系边列表（可按实体过滤） |
| POST / DELETE | `/relations`、`/relations/{id}` | 手工维护关系边 |
| GET | `/neighbors?entity=` | 某知识点的知识库邻域（证据下钻） |
| POST | `/upload` | 上传知识库文档 → 理解 → 入库（`dry_run=true` 只出报告） |
| POST | `/preview` | 粘贴文本理解（默认只预览） |
| GET | `/imports`、`/imports/{id}` | 上传历史 / 单次完整理解报告 |
| POST | `/match` | 知识锚定：文本命中了哪些知识点 + 覆盖率 |
| GET | `/graph` | 知识库本体图（点 + 边，供 D3 渲染） |
| GET | `/files` | 文件知识画像 + 文件间知识关联（含证据） |
| GET | `/file/{file_id}` | 单文件画像详情（概念邻域、相关文件与证据） |
| POST | `/rebuild` | 重建：关系推导 → 锚定 → 画像 → 文件关联 → 节点关联 |

---

## 六、用户上传知识库：能理解什么

支持 5 种格式：

| 格式 | 识别方式 |
|---|---|
| Markdown 条目式 | `## 实体 (别名)` + `别名:` / `关系:` / `bridge_sentence:` / `domain:` / 正文定义 |
| Markdown 表格 | `\| 实体 \| 别名 \| 领域 \| 定义 \|` |
| CSV / TSV | 表头含 实体/别名/领域/层级/定义/关系（**兼容 Excel 导出的 BOM**） |
| JSON | `[{"entity": "...", "definition": "..."}]` 或 `{"entries": [...]}` |
| 纯文本 | 每行 `实体：定义` |

理解报告四类结论：

1. **新增知识点** —— 知识库没有的条目，入 `knowledge_base`（来源标记 `upload:文件名`）。
2. **同义合并** —— 与既有条目同义（命中规范名或别名）时，合并别名、相关术语、桥接句，不产生重复节点。
3. **定义冲突** —— 同名但定义差异大（字符二元组 Jaccard < 0.25），默认保留知识库既有定义并记录冲突，可选择"上传内容覆盖"。
4. **未采纳** —— 实体名过短/无有效字符/疑似链接/信息量不足的行，附带原因，不污染知识库。

导入后自动：写入关系边（`origin='import'`）→ 推导本体关系 → 重新计算文件画像与关联。

示例文件：`test_data/kb_demo/kb_sample_web_dev.md`、`test_data/kb_demo/kb_sample_web_perf.csv`。

---

## 七、实测效果（5 份演示文档）

| 文件 | 锚定次数 | 知识点数 | 覆盖率 |
|---|---|---|---|
| 前端工程实践.txt | 14 | 7 | 34.9% |
| 技术选型讨论.txt | 20 | 12 | 43.9% |
| 服务端接口设计.txt | 11 | 9 | 23.1% |
| 写作与知识整理.txt | 3 | 2 | 3.8% |
| 自造概念实验.txt（自造"彩虹排序法"） | 1 | 1 | 1.4% |

文件知识关联结果：

- 前端工程实践 × 技术选型讨论 = **0.43**（共享 Vue / TypeScript / React / Webpack / 前端开发）
- 技术选型讨论 × 服务端接口设计 = **0.41**（共享 Node.js / RESTful API / MySQL / 后端开发）
- 自造概念实验 × 任意文件 = **不连**（核心概念不在知识库内 → 按知识边界不参与关联）

对比修复前：同一批语料曾产生 9 条关联（含"体裁 ← 动态弱类型"这类误锚定造成的噪音连线），
修复泛化别名与枢纽降权后收敛为 3 条可解释关联。

---

## 八、使用方式

### 后端

```bash
cd backend
py -m uvicorn app.main:app --host 127.0.0.1 --port 8080   # 启动时自动：种子知识库 + 关系边构建
```

命令行工具：

```bash
py scripts/kb_build.py                       # 关系推导 + 全量锚定/画像/关联
py scripts/kb_build.py --relations-only      # 只推导知识库关系边
py scripts/kb_build.py --sample "一段文本"    # 抽样看锚定结果
py scripts/kb_build.py --understand test_data/kb_demo/kb_sample_web_dev.md
```

### 前端

打开「知识库」标签页（`http://127.0.0.1:5173` → 顶部导航"知识库"）：

- **概览**：知识点/关系/覆盖率统计、领域与层级分布、知识库本体图（可按领域过滤、按领域或层级着色）。
- **上传理解**：拖入知识库文件 → 先出预览报告 → 确认后导入；下方是上传历史，点任意一行可回看完整报告。
- **知识点**：搜索/筛选/新增/编辑/删除；点知识点名称可看它在知识库中的邻域与证据。
- **知识关联**：锚定测试（粘贴文本看命中哪些知识点）→ 文件知识画像卡片 → 文件间关联（含共享知识点与知识库通路证据）→ 未覆盖文件告警。

顶部"重建知识关联"按钮 + 阈值滑条：改完知识库后一次性重算全链路。

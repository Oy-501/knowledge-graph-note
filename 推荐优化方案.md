# 推荐优化方案

> 本文档由项目维护者整理，用于记录当前版本（V1.0）中已识别但尚未实施的功能缺口与推荐优化方向。
> 文档中带 [x] 的为已实施项，带 [ ] 的为待实施项。

## 一、同文件内知识点连线（最高优先级）

- [ ] 当前 `backend/app/services/inference.py` 只做「新文件 × 其他文件旧节点」的两两配对。
- [ ] 结果：只上传 1 个文件时，同文件内不同知识点之间永远不会建立连线，图谱显示为 1 个孤立文档节点，用户感知为「知识点联系低 / 图谱空白」。
- [ ] 推荐做法：在 inference 中增加「同文件内配对」逻辑，新文件解析出的知识点两两之间也参与打分，跨主题的知识点（如「Redis 缓存」与「JVM 内存」）可通过向量/知识库桥接建立同文件内部边，并标记 `same_file: true`。
- [ ] 影响面：`inference.py` 的 `run_inference_for_doc` 与 `_score_all_pairs`；`parser` 的 chunk 抽取粒度建议同时下调（当前 1000 字符分片，可改为按语义段落）。

## 二、前端「强制重排全量图谱」调错接口

- [ ] 当前 `kg-vue3/src/components/ControlPanel.vue` 的「立即重排全量图谱」按钮调用的是 `POST /api/graph/build`，而该接口只处理「增量构建（上传新文件）」。
- [ ] 后端已存在 `POST /api/graph/rebuild`（全量重建），但前端没有调用它。
- [ ] 推荐做法：将按钮改为调用 `POST /api/graph/rebuild`，并在重建期间显示进度（后端已有 `GET /api/tasks/{id}` 轮询接口）。
- [ ] 影响面：`ControlPanel.vue` 一处请求方法 + 一个 loading 状态。

## 三、知识点校验：建议增加「时效性/事实性」维度

- [ ] 当前 validator 已覆盖 7 类 × 4 级：语法、语义、逻辑、完整性、一致性、规范性、价值观。
- [ ] 对涉及版本/时间的知识点（如「Spring Boot 2.x 已停止维护」），建议增加事实核查维度，将「过时」与「错误」区分提示。
- [ ] 影响面：`backend/app/services/validator.py` 增加一个 rule；前端 `ValidationPanel` 增加过滤标签。

## 四、图谱可视化性能

- [ ] 当前 D3 力导向图在节点数 > 800 时会出现卡顿。
- [ ] 推荐做法：引入 Web Worker 运行 simulation；或使用 `simulation.alphaDecay` 动态调参；或在大图时切换到「分组聚类视图」（按 domain 聚合）。
- [ ] 影响面：`GraphCanvas.vue` 的 simulation 配置与渲染循环。

## 五、知识库导入：支持 Markdown 全文导入

- [ ] 当前 kb_import 支持 JSON/CSV 结构化导入；.md 文件需要先转成 JSON。
- [ ] 推荐做法：扩展 parser 复用「分片抽取」逻辑，将 .md 直接解析为知识点并去重后入库。
- [ ] 影响面：`backend/app/services/kb_import.py` 增加 md 分支。

## 六、本地文件打开：Electron 深度集成

- [ ] 当前「打开本地文件」走浏览器原生 file picker（HTML5），只能读取文本；大文件（>10MB）解析会卡住主线程。
- [ ] 推荐做法：Electron 主进程 fs 读取 + 异步分片解析，通过 IPC 把内容按块传给渲染进程。
- [ ] 影响面：`main.js`（主进程 IPC）、`preload.js`（桥接）、`app.js`（渲染进程消费）。

## 七、数据导出：图谱快照

- [ ] 支持将当前图谱导出为 PNG/SVG 快照，便于放进文档/汇报。
- [ ] 影响面：`GraphCanvas.vue` 增加 `svg2png` 导出按钮。

## 八、多用户与云同步（远期）

- [ ] 当前所有数据存于浏览器 IndexedDB，无法跨设备。
- [ ] 推荐做法：后端增加同步端点（GET/PUT /api/sync/{userId}），前端在 Pinia 中做冲突合并。
- [ ] 影响面：新增 store + 新增 API。

# 开发环境：后端 uvicorn app.main:app 运行在 8080（8000 被 Windows 系统占用）
#
# 刻意写 127.0.0.1 而不是 localhost：
# 浏览器解析 localhost 时可能优先取 IPv6 的 ::1，而后端只绑 IPv4 的 127.0.0.1，
# 于是请求被拒（浏览器报 Failed to fetch），前端所有 GET 静默失败 ——
# 界面上表现为「知识点 0、文件 0」，用户完全看不出是加载失败还是真没数据。
# 用 127.0.0.1 明确指定 IPv4，消除这层解析歧义。
VITE_API_BASE=http://127.0.0.1:8080/api

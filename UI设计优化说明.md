# UI 设计优化说明

> 这一轮不是凭感觉"美化"，而是先**用证据定位真实问题**，再修。
> 改动都经过实测验证；两件事因为实测而推翻了最初的假设（文档里有说明）。

---

## 一、先说结论

| 优先级 | 问题 | 性质 | 状态 |
|---|---|---|---|
| **P0** | 知识库页显示「知识点 0 / 关系 0」，实际后端有 404 条 | **真实缺陷**（用户会误以为数据丢了） | 已修 |
| **P0** | 每次打开应用都弹出管理口令框 | **真实缺陷**（UX 严重受损） | 已修 |
| **P1** | 整站全是"注释级小字"：组件内 395 处字号声明里 **235 处小于 12px**（10px 62 处、9px 24 处） | 可读性系统性问题 | 已修 |
| **P2** | 空态风格割裂：4 处用 Element Plus 默认的灰色 3D 方块插图，1 处用 emoji 🧠 | 一致性 | 已修 |
| **P2** | 同类「保存」按钮两种样式（实心 vs 描边） | 一致性 | 已修 |
| **P2** | 加载失败被渲染成一整套"0"，无法与"真的没有数据"区分 | 真实性/可达性 | 已修 |

---

## 二、P0-1：知识库页"0"其实是加载失败（不是数据没了）

### 现象

知识库页 5 张统计卡全显示 `— / 0`，但后端 `/api/kb/stats` 返回 `知识点 404 / 关系 1489`。

### 根因（比表面深一层）

前端 `VITE_API_BASE=http://localhost:8080/api`。排查发现：

- `localhost` 在本机浏览器里被解析到 **IPv6 `::1`**；
- 而 `::1:8080` 上监听的是**另一个完全无关的应用 —— LocalAI**（它以通配符 `0.0.0.0:8080` + `[::]:8080` 绑在全网卡上）；
- 于是前端的请求**打到了 LocalAI**，所有知识库接口自然 404/失败。

所以这不是"偶尔连不上"，而是**端口被另一个应用占用 + localhost 解析歧义**叠加。

### 修法

1. `kg-vue3/.env.development` 与 `api/index.js` 的兜底值统一改为 **`http://127.0.0.1:8080/api`**
   —— 明确指定 IPv4，消除解析歧义，确保只打到本后端。
   （顺带修正了兜底值里写错的端口 `localhost:8000`，实际是 8080。）
2. `kbStore.refreshAll()` 从 `Promise.all` 改为 **`Promise.allSettled`**，
   并把每个模块的失败记录在 `loadErrors` 里（原来一处失败全盘静默，
   页面照常渲染一整套"0"）。
3. **加载失败要显式说明，不能渲染假的"0"**（见 P2-6）。

### 实测

```
修复前：统计卡全 "—/0"
修复后：404 知识点 · 562 个别名可锚定
        1489 知识库关系 · 推导边 2631 条
        1/1 已覆盖文件 · 覆盖率 100%
        22 平均覆盖知识点
```

> **需要你知道的事**：`192.168.1.223:8080`（本机局域网 IP）上也能访问到 LocalAI。
> 那是另一个应用，不是本项目的；但它以全网卡监听，属于暴露面。
> 如果你不需要它对外，建议检查它的启动配置。

---

## 三、P0-2：每次打开应用都要输口令

### 现象

页面一加载就弹出「需要管理口令」。

### 根因

后端日志直接指认：

```
拒绝未授权写操作：POST /api/graph/build
拒绝未授权写操作：POST /api/summary/build
```

两处：

1. 启动时 `loadFromBackend()` 用 `POST /api/graph/build` 来**读取**图谱
   —— 而这个接口只是查库拼响应，**根本不写**（纯查询，名字误导）。
2. 总结页 `onMounted` 里**自动生成一次总结**（`POST /api/summary/build`）
   —— 每次打开页面都替用户跑一遍昂贵计算。

上一轮加了写操作守卫（POST/PUT/DELETE 需口令），这两个"读语义的 POST"
就被拦下了。

### 修法（修正动词，而不是豁免）

- **读操作就用 GET。** 后端新增 `GET /api/graph`（只读），
  前端 `graphStore.loadFromBackend()` 改用它。`POST /build` 保留兼容，仍受保护。
  豁免会削弱规则，修动词才是对的。
- **总结页不再进入就自动生成**。生成是有实际成本的操作，应由用户主动触发；
  现在进入页面先显示上次结果（store 常驻），没有就显示带「立即生成总结」按钮的空态。

### 实测

遍历 6 个页签后，后端收到的请求全是 GET（13 个），**0 个写操作被拦**：

```
GET /api/graph  ·  GET /api/files  ·  GET /api/files/limits
GET /api/notes  ·  GET /api/notes/1  ·  GET /api/profile  ·  GET /api/profile/overview
GET /api/kb/stats · entries · relations · graph · files · imports
被拦写操作：0
```

---

## 四、P1：建立字号体系，消灭"注释级小字"

### 证据

组件内 395 处 `font-size` 声明的分布触目惊心：

```
11px × 120   12px × 83   10px × 62   9px × 24   11.5px × 22
9.5px × 1    10.5px × 6   12.5px × 19   13px × 14   14px × 12   16px+ ...
→ 235 处小于 12px，其中 93 处小于 11px
```

十几种手写字号（8/9/9.5/10/10.5/11/11.5/12/12.5/13/13.5px），
全站读起来都是"注释级"文本，没有排版层级，也没法整体调。

### 修法

1. 在设计令牌里建立 **6 级字号体系**：

   | Token | 值 | 用途 |
   |---|---|---|
   | `--fs-xs` | 11px | 徽标/角标/快捷键提示（下限，仅极短文本） |
   | `--fs-sm` | 12px | 次级信息：统计说明、时间戳 |
   | `--fs-md` | 13px | 正文与标签：列表项、表单标签、按钮文字 |
   | `--fs-base` | 14px | 正文基准 |
   | `--fs-lg` | 16px | 小标题 |
   | `--fs-xl` | 18px | 区块标题 |

2. 用脚本把 28 个文件里的 503 处声明**统一映射到体系**：
   **469 处**归入级别名，其中 **124 处小于 11px 的被抬升到可读下限 11px**；
   ≥15px 的展示级字号保留不动。

   下限定在 11px 的理由：10px 及以下在 1x 屏上已接近不可读，
   且中文小字比拉丁字母更吃力。

3. 收益不只是"变大"：以后想整体调整，只改 6 个变量即可。

### 实测

- 构建通过；**横向溢出扫描为 0**（字号变大最常见的回归是撑破布局）。
- 导航、统计卡、列表、按钮的可读性明显提升（对比改动前后截图）。

---

## 五、P2-1：统一空态

### 问题

空态各写各的：4 处用 Element Plus 默认的**灰色 3D 方块插图**
（与全站暖色扁平风格明显冲突），图谱页用的是 **emoji 🧠**
（跨平台渲染不一致，Windows 上还会退化成彩色表情符号）。

### 修法

新建 `AppEmpty.vue` 统一空态：柔和的**线性图标**（三个节点 + 连线 + 虚线底座，
呼应"知识图谱"的意象，而不是通用占位图）+ 主文案 + 可选说明 + 可选操作按钮。
替换全部 5 处。

> 顺带把空态里的"引导语"从"告诉用户按钮在哪"升级为"**直接给一个可点按钮**"
> （总结页空态现在带「立即生成总结」按钮），比只写"点击右上角"更直接。

---

## 六、P2-2：统一按钮层级

约定：**每张卡片的主操作（如"保存"）用 `primary` 实心；次级操作（上传/应用/移除）用默认描边；同一卡片内按钮尺寸一致。**

个人主页里「保存资料」与「保存背景设置」同为卡片主操作，此前一个实心一个描边，
且「保存资料」是默认尺寸而同行按钮是 small。已统一为 `type="primary" size="small"`。

> **一个验证教训**：我曾凭截图判断两个按钮"颜色不同"（一蓝一绿），
> 用 `getComputedStyle` 核实后发现**两个都是 `rgb(79,111,143)`** ——
> 是截图缩放+背景色对比造成的观感误差。
> **这类颜色结论不能靠截图，要用计算样式核实。**（本轮出现两次，都纠正了。）

---

## 七、P2-6：加载失败要有明确的错误态

### 问题

原来加载失败只弹一个会自动消失的 toast，页面照常渲染出一整套"0"。
**「知识点 0」和「加载失败」在视觉上完全一样**，用户会把"后端没连上"
误读成"我的知识库空了"——这比报错更糟。

### 修法

1. store 里增加 `loadErrors`，记录每个模块的失败原因；
   汇总成一条提示而不是 6 个 toast 刷屏。
2. 概览页显示**错误横幅**：红色色条 + 图标 + 原因 + **可执行建议**
   （告诉用户检查 `启动服务.bat` 和 `VITE_API_BASE` 端口）+ **重试按钮**；
   下方统计卡整体降噪（`is-stale`，避免误读），数值显示「尚未加载」而不是假的 0。

### 实测（注入故障 → 恢复）

```
让 /api/kb/stats 失败一次：
  → 出现横幅「概览数据加载失败，下方数字不可信」，原因 "模拟：首次统计接口不可用"
点击「重新加载」：
  → 横幅清除、降噪解除、恢复 "404 知识点 · 1489 知识库关系"
```

---

## 八、实测过程里发现的一个测试陷阱（值得记住）

验证重试按钮时，我在浏览器里反复"杀/起后端进程"，导致 Chromium 缓存了
**对 `127.0.0.1:8080` 的连接失败**——之后 `curl` 全部 200，浏览器却
一直 `Failed to fetch`。重启浏览器即恢复。

**这是浏览器侧的连接状态污染，不是应用缺陷**；而且恰好验证了
错误态在真实断连下是对的。

另一个：Vite HMR 会带时间戳查询参数，导致同一模块存在**两个实例**。
我用 `import('/src/api/index.js')` 打补丁打到了另一个实例上，
一直没生效。改用"组件实例真正持有的那个对象"才生效
（`与import的是同一个: false` 证实了这一点）。

---

## 九、本轮改动清单

| 文件 | 改动 |
|---|---|
| `kg-vue3/.env.development`、`kg-vue3/src/api/index.js` | API 地址统一 `127.0.0.1:8080`，修正兜底端口 |
| `kg-vue3/src/store/kbStore.js` | `Promise.allSettled` + `loadErrors` 失败记录 + 汇总提示 |
| `kg-vue3/src/components/KnowledgeBaseView.vue` | 加载失败横幅（原因+建议+重试）+ 卡片降噪 |
| `backend/app/api/graph.py` | 新增只读 `GET /api/graph`；`POST /build` 保留兼容 |
| `kg-vue3/src/store/graphStore.js` | 改用只读 `GET` 读图谱 |
| `kg-vue3/src/components/GraphSummaryView.vue` | 去掉进入页面自动生成；空态带生成按钮 |
| `kg-vue3/src/styles/main.css` | 新增 6 级字号体系（`--fs-*`） |
| `kg-vue3` 26 个文件 | 503 处字号映射到体系，124 处抬升到 11px |
| `kg-vue3/src/components/AppEmpty.vue` | **新增**：统一空态组件 |
| `kg-vue3/src/components/{AdminView,GraphSummaryView,GraphCanvas}.vue` | 5 处空态替换为 AppEmpty |
| `kg-vue3/src/components/ProfileView.vue` | 统一两个保存按钮的层级与尺寸 |

---

## 十、未做（如实说明）

- **没有改整体配色/视觉风格**（没动 雾霾蓝/薄荷绿/暖杏 的品牌基调）——
  本轮聚焦"真实缺陷 + 一致性 + 可读性"。如果你想换一套视觉（更简洁 /
  更高对比 / 深色优先），那是另一个量级的改版，需要先定方向。
- **图谱总结的页头描述段**仍是两行灰字，可以精简到一行（提示与操作分离），未改。
- **知识库领域分布的域名**较长会被截断（有 tooltip），未改成自适应换行。
- **图谱页左侧「文件管理」与「文件上传」** 两个标题语义有重叠，未合并。
- 没有给工作台编辑区做更大胆的排版（如焦点模式、字号跟随系统设置），未改。
